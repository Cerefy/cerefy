apiVersion: v1
kind: Service
metadata:
  name: {{ include "cerefy.fullname" . }}
  labels:
    {{- include "cerefy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cerefy.selectorLabels" . | nindent 4 }}
