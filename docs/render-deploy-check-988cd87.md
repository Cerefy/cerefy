
## Manual deploy action

The Render service page exposed `Manual Deploy` with `Deploy latest commit`, `Deploy a specific commit`, `Clear build cache & deploy`, and `Restart service`. The `Deploy latest commit` option was selected for the current `main` branch. Render returned to the service/deploy page, but no new deployment ID or final status is visible yet.
